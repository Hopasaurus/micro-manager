---
doc: detail
id: T-0227
title: Columns M1: Go v2 support + 1->2 migration step (fixtures only)
updated: 2026-08-20
---

# T-0227 — Columns M1: Go v2 support + 1->2 migration step (fixtures only)

Milestone 1 of notes/add-columns-plan.md. Depends on M0 (spec must land first). Builds board.md read/write/validate, generalized Stage type, and the 1->2 migration step in the Go library, tested only against sample-data/ and new v2 fixtures. The live board (implementations/golang/micro-manager -- this board) is NOT touched in this task. Full checklist: notes/add-columns-plan.md#m1.

## Notes

Scoped down to a 'core slice' per user's explicit pacing choice (AskUserQuestion): item.go (Stage/StageConfig/StateBoard, additive), a new board.go parser/writer for board.md, store.go/validate.go/tx.go wiring, and --add/--start/--pause/--finish/--move generalized for v2 in a new board_ops.go, all dispatched via an early 'if t.model.isV2()' branch so every existing v1 code path and test is 100% untouched (verified: full v1 suite passes unchanged). New board_test.go (26 tests) covers parsing, referential integrity, WIP caps, needs_reason, tickler_stages placement, and all five operations against a hand-built v2 fixture (custom 'review' stage included). Found and fixed two real bugs while testing: moveV2 wasn't stamping started: on entering working (spec-tools.md corrected to match); Appendix B in spec-gui.md had a malformed entry that broke the spec-conformance test suite (fixed) -- also fixed a stale done.md version-field value in spec-file-format.md left over from T-0226. Deferred to a follow-up task: op_tick.go, migrate.go, op_update.go and the remaining peripheral op_*.go/list.go/report.go files, and additional v2 fixtures under sample-data/. Full test suite: mm package 100% green (build+vet+gofmt+test); internal/web has pre-existing, expected failures from the T-0226 spec patch (dynamic columns, item-field-stage/reason, settings-migrate) that are M4's job, not touched here.

---
doc: detail
id: T-0235
title: Columns M1f: op_addmany.go v2, remaining --edit gaps, more v2 fixtures, --init v2 question
updated: 2026-08-20
---

# T-0235 — Columns M1f: op_addmany.go v2, remaining --edit gaps, more v2 fixtures, --init v2 question

The rest of T-0234's checklist, deferred out of its scope (op_tick.go v2 was the session's chosen headline item). T-0234 shipped: Tick()/Ticklers() generalized for board.md — any stage named as a tickler_stages SOURCE is now eligible (not just ## Someday), each fire's destination is the item's own tickler_dest override or its stage's tickler_stages default (not a hardcoded ## Ready), a WIP-capped destination is checked explicitly before firing (§10.4: no invariant catches an overfilled cap, so nothing else would), and Tickler/FiredTickler both gained a Dest field per spec-tools.md §6.1's pseudocode. New tickv2_test.go: one-shot to the stage default, one-shot to an item's own override, a recurring spawn's no-copy rules (no reason:, no detail:, no extras — same as v1), the Ticklers() listing's Dest field, the WIP-cap refusal (reported as a per-item TickError, never aborts the run — an item blocked by capacity stays scheduled and keeps trying on later runs, tickled: never stamped), a malformed tickler_dest never aborting the run, and dry-run-matches-real-run (noted: only true absent capacity contention within one run — a dry run's iterations each re-read from disk, so they cannot see an earlier, unwritten fire in the same dry run consuming capacity the way a real run's committed fires do; not something this session changed or claims to fix). internal/cli's --tick needed no changes at all: runTick already calls Store.Tick generically, so it works against a v2 board automatically. Full suite: mm + internal/cli 100% green; internal/web's pre-existing T-0226 failures untouched, still M4's job. Deferred to T-0235: op_addmany.go's v2 bulk-add path, a couple of smaller op_update.go rough edges (tickler placement check via StageConfig, --set stage: routing), additional v2 test fixtures parallel to sample-data/sample1-3, and whether --init needs a direct-to-v2 path.

See notes/add-columns-plan.md#m1 and T-0227/T-0231/T-0232/T-0233's detail files for what already landed.

## Notes

**2026-08-20** — Shipped: op_addmany.go's v2 bulk-add path (addManyV2 in board_ops.go, mirroring addV2 for a stage's run instead of a section's; ParseAddLine/applyAddField gained stage:/reason:/tickler_dest: cases). While testing it, found and fixed a real, independent bug in board.go's InsertItem, dormant since T-0227: b.Items was append-only, so a SECOND InsertItem call within one transaction (only possible via a bulk operation — every existing single-item v2 op calls InsertItem once) would see StageItems() return items in insertion order rather than file order, corrupting position math for every subsequent insert in the same batch. Fixed by keeping b.Items sorted by Source.Line on every insert. Also fixed op_update.go's setAnyField 'tickler' case: it checked it.Section != SectionSomeday unconditionally, which is always true for a v2 item (Section is always empty), so --set tickler:X was refused on every v2 item regardless of its stage; now checks the directory's tickler_stages/it.Stage for v2, matching op_tick.go/addV2/moveV2's existing placement logic. New tests: addmanyv2_test.go (6 tests) and one in v2ops_test.go for the tickler fix. Full suite: mm + internal/cli 100% green; internal/web's pre-existing T-0226 failures untouched. Remaining items (--set stage: routing, op_wip.go v2 messaging, --init's v2 question, more v2 fixtures) don't block M2/M3 and are operation-surface completion — folded into T-0230 (M4) rather than a new M1 sub-task, to stop the T-0231..T-0235 chain here.

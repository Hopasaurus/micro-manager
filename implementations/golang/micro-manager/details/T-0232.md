---
doc: detail
id: T-0232
title: Columns M1c: CLI --migrate wiring, op_tick.go, op_update.go, remaining v2 op coverage
updated: 2026-08-20
---

# T-0232 — Columns M1c: CLI --migrate wiring, op_tick.go, op_update.go, remaining v2 op coverage

The rest of M1's original checklist, deferred out of T-0231's scope (mm/migrate.go's chained --migrate mechanism and the 1->2 step, plus its round-trip test, shipped in T-0231). Blocks M2 (T-0228), which needs `mm --migrate --dry-run` to work from the command line, not just the library call. Remaining:

- CLI wiring for the new `Store.MigrateVersion`/`MigrationStep`/`MigrationResult` chain under `--migrate` (internal/cli), replacing or sitting alongside the legacy T-0044 fixups it currently runs; this is also where the git-uncommitted-changes warning (spec-tools.md §5.3.4) belongs, since the mm library itself must not run programs (TestLibraryImports forbids os/exec there)
- op_tick.go generalized for tickler_stages (SOURCE->DEST) and tickler_dest, for both fire kinds
- op_update.go (--edit) generalized for stage:/reason:/tickler_dest:
- the remaining peripheral op_*.go files (op_addmany, op_archive, op_detail, op_remove, op_stats, op_wip, list.go, report.go, search.go, status.go) at least compile-level v2 support
- additional v2 test fixtures beyond the one hand-built board_test.go fixture and migrate_test.go's migration fixture, parallel to sample-data/sample1-3

See notes/add-columns-plan.md#m1 and T-0231's detail file for what already landed.

---
doc: detail
id: T-0232
title: Columns M1c: CLI --migrate wiring, op_tick.go, op_update.go, remaining v2 op coverage
updated: 2026-08-20
---

# T-0232 — Columns M1c: CLI --migrate wiring, op_tick.go, op_update.go, remaining v2 op coverage

The rest of M1's original checklist, deferred out of T-0231's scope (mm/migrate.go's chained --migrate mechanism and the 1->2 step, plus its round-trip test, shipped in T-0231). Blocks M2 (T-0228), which needs `mm --migrate --dry-run` to work from the command line, not just the library call. Remaining:

- CLI wiring for the new `Store.MigrateVersion`/`MigrationStep`/`MigrationResult` chain under `--migrate` (internal/cli), replacing or sitting alongside the legacy T-0044 fixups it currently runs; this is also where the git-uncommitted-changes warning (spec-tools.md §5.3.4) belongs, since the mm library itself must not run programs (TestLibraryImports forbids os/exec there)
- op_tick.go generalized for tickler_stages (SOURCE->DEST) and tickler_dest, for both fire kinds
- op_update.go (--edit) generalized for stage:/reason:/tickler_dest:
- the remaining peripheral op_*.go files (op_addmany, op_archive, op_detail, op_remove, op_stats, op_wip, list.go, report.go, search.go, status.go) at least compile-level v2 support
- additional v2 test fixtures beyond the one hand-built board_test.go fixture and migrate_test.go's migration fixture, parallel to sample-data/sample1-3

See notes/add-columns-plan.md#m1 and T-0231's detail file for what already landed.

## Notes

**2026-08-20** — Shipped (the 'Core correctness fixes' slice): (1) op_detail.go's writeItemLine gained a StateBoard case, which unlocks Update/AttachDetail/DetachDetail/SetDetailBody/AppendDetailSection for v2 items — previously every one of them failed with 'has no state'. (2) op_remove.go's Remove() gained the same StateBoard case. (3) store.go: Filter gained a Stage field; List()'s default-state logic was v1-only (defaulted unset State to StateBacklog, which no v2 item ever has) — a bare mm --list on a v2 board returned nothing at all, now fixed to default to StateBoard when the directory is v2; Filter.Blocked now also honors reason:. (4) status.go: Status gained StageCounts (per declared stage, zero-filled); Status()/Next() generalized via a new readyEquivalent() helper (the v1 ## Ready section, or a v2 board's 'ready' stage when declared) instead of hardcoded SectionReady, so both now work on a v2 board. (5) op_stats.go and report.go were only reading Project from backlog.md, leaving it blank on every v2 Stats/Report call — fixed. (6) Confirmed via new tests that search.go and op_archive.go needed no changes at all — already state-agnostic. (7) op_update.go: fixed the --blocked flag routing to reason: for v2 items instead of writing a legacy blocked: field; along the way a test caught setAnyField/unsetAnyField missing 'reason'/'tickler_dest' cases entirely (any v2 item that already had a value there would get the field written TWICE on one line via the --set escape hatch, a duplicate the transaction's own reparse safety net caught and refused rather than corrupting the file — but the operation itself failed) — fixed both. New v2ops_test.go: 20 tests. Full suite: mm + internal/cli 100% green; internal/web's pre-existing T-0226 failures untouched, still M4's job. Deferred to T-0233: CLI --migrate wiring (the actual reason M2/T-0228 is still blocked), op_tick.go, op_addmany.go's v2 path, a couple of smaller op_update.go rough edges (tickler placement check, --set stage: routing), and more v2 fixtures.

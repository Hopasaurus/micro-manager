---
doc: detail
id: T-0228
title: Columns M2: prove the migration step against a copy of the live board
updated: 2026-08-20
---

# T-0228 — Columns M2: prove the migration step against a copy of the live board

Milestone 2 of notes/add-columns-plan.md. Depends on M1. Runs mm --migrate --dry-run and then for real against a branch/scratch copy of implementations/golang/micro-manager, checks the result, and fixes whatever the real board's ~200 items surface that synthetic fixtures didn't. Full checklist: notes/add-columns-plan.md#m2.

## Notes

**2026-08-20** — Now blocked by T-0232, not directly by T-0231 (finished): M2's 'mm --migrate --dry-run against a copy' needs the new chained --migrate wired into the CLI, which T-0231 deliberately left as library-only (mm/migrate.go: Store.MigrateVersion, proved by migrate_test.go, not yet reachable from the command line).

**2026-08-20** — Blocking task is now T-0233, not T-0232 (finished): T-0232 shipped List/Status/Next/Remove/Update/AttachDetail correctness fixes for v2 but deliberately deferred the CLI --migrate wiring itself.

**2026-08-20** — T-0233 shipped: mm --migrate now reaches the new chained mechanism from the command line (mm --migrate --dry-run works). T-0228/M2 is unblocked and ready to pick up.

**2026-08-20** — Assessed for splitting (as asked) rather than pre-split blind: M2's checklist is inherently sequential and exploratory (dry-run, real-run, spot-check, THEN fix whatever surfaces) — its last item can't be decomposed in advance since what needs fixing is unknowable until the run happens. So instead of splitting, ran the checklist itself against a throwaway /tmp scratch copy of the live board (never touched the real implementations/golang/micro-manager): freshly built mm (the installed /usr/local/bin/mm predates T-0233's --migrate wiring), --migrate --dry-run (22 changes, clean), --migrate for real, --check (clean: 20 in backlog, 200 done, no violations). All 4 working slots were idle at run time, so the notes/subtasks/blockers-folding path had nothing to spot-check live — already covered synthetically by mm/migrate_test.go's dedicated fixture (two working items, one with an existing detail file, one without). Real-world stress test the synthetic fixtures didn't cover: several blocked items' reason: values contain colons and heavy punctuation (e.g. T-0132's 'NO-GO from T-0131 (2026-08-02): premise unmet - idle traffic is 100% backstop...') — survived the field-value round trip correctly, confirmed by the clean --check afterward. One real, concrete finding: mm --status (and its --json form) hardcode Status.Ready/Blocked/Someday, which stay 0 for a v2 directory by design (T-0232 added StageCounts instead) — so on the real migrated board, --status printed 'ready: 0, blocked: 0, someday: 0' despite 20 open items. This is CLI-surface work, not a migration-correctness bug, so it's noted on T-0230 (M4) rather than fixed here or split off as its own M2 sub-task. Conclusion: the migration step is proven against real data. Not splitting T-0228; marking it finished.

---
doc: detail
id: T-0231
title: Columns M1b: migrate.go, op_tick.go, remaining v2 op coverage
updated: 2026-08-20
---

# T-0231 — Columns M1b: migrate.go, op_tick.go, remaining v2 op coverage

The rest of M1's original checklist, deferred out of T-0227's 'core slice' scope (item.go/board.go/validate.go/start+pause+finish+move+add, all shipped). Blocks M2 (T-0228), which needs migrate.go to exist. Remaining: op_tick.go generalized for tickler_stages (SOURCE->DEST) and tickler_dest, for both fire kinds; new migrate.go with a registered [](MigrationStep) chain and the 1->2 step (fold working.NN.md into board.md via stage:, move ## Notes/## Plan/## Blockers into details/<ID>.md, rename backlog.md to board.md, git-uncommitted-changes warning); a migration round-trip test against a captured v1 board with real notes/subtasks/blockers; op_update.go (--edit) generalized for stage:/reason:/tickler_dest:; the remaining peripheral op_*.go files (op_addmany, op_archive, op_detail, op_remove, op_stats, op_wip, list.go, report.go, search.go, status.go) at least compile-level v2 support; additional v2 test fixtures beyond the one hand-built board_test.go fixture, parallel to sample-data/sample1-3. See notes/add-columns-plan.md#m1 and T-0227's detail file for what already landed.

## Notes

**2026-08-20** — Shipped: mm/migrate.go — a general, registered, chained migration mechanism (MigrationStep/MigrationResult, spec-tools.md §5.3.4) plus the 1->2 step itself: backlog.md+working.NN.md fold into board.md, blocked: renames to reason:, wip.working is synthesized from the working-file count, every other backlog.md frontmatter key (including unregistered ones) carries through verbatim, done.md's version: bumps in place with everything else byte-identical, and each working item's ## Task/## Plan/## Notes/## Blockers body folds into details/<ID>.md (creating it when the item has none, appending to it when it already exists) via the existing sectionBody/appendUnderHeading primitives. The result is validated as a real version-2 dirModel before anything is written, and the legacy T-0044 op_migrate.go (working.md rename, missing project, flow-sequence tags) is untouched and orthogonal. New migrate_test.go proves it end to end against a captured v1 fixture with real notes, subtasks and a blocker in two working files (one with no prior detail file, one with an existing one to append to), plus dry-run-writes-nothing and re-migrate-is-ErrConflict cases. mm package: build+vet+gofmt+test all green; internal/web's pre-existing T-0226 failures are untouched, still M4's job. Deferred to T-0232: CLI --migrate wiring (including the git-dirty warning, which the library itself cannot do — TestLibraryImports forbids os/exec), op_tick.go, op_update.go, the remaining peripheral op_*.go files, and more v2 fixtures.
